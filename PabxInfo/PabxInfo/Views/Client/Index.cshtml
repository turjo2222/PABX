@model PabxInfo.Models.ClientAddViewModel
@{
    Layout = null;
}
@*@if (ViewBag.SuccessMsg != null)
    {
        <script type="text/javascript">
                window.onload = function () {
                    alert("@ViewBag.Message");
                };
        </script>
    }*@


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />

</head>
<body>
    <div>
        <form class="form-horizontal" id="myForm" method="POST">
            <div class="contetainer">
                <div class="row">
                    <div class="col-lg-3"></div>
                    <div class="col-lg-6">
                        @if (ViewBag.SuccessMsg != null)
                        {
                            <h3 style="font-family: cursive; color: cadetblue">@ViewBag.SuccessMsg</h3>
                        }

                        @if (ViewBag.FailMsg != null)
                        {
                            <h3 style="font-family: cursive; color: brown">@ViewBag.FailMsg</h3>
                        }
                    </div>
                    <div class="col-lg-3"></div>

                </div>
                <div class="row">
                    <div class="col-lg-3"></div>
                    <div class="col-lg-3">

                        <div class="form-group">
                            @*<label class="control-label col-sm-2" for="Address">Address:</label>*@
                            @Html.LabelFor(c => c.ClientName, null, new { @class = "control-label col-sm-2" })
                            <div class="col-sm-10">
                                @*<input type="text" class="form-control" id="Address" name="Address" placeholder="Address">*@
                                @Html.TextBoxFor(c => c.ClientName, null, new { @class = "form-control", placeholder = "Name" })

                            </div>
                        </div>
                        <div class="form-group">
                            @Html.LabelFor(c => c.DesignationID, null, new { @class = "control-label col-sm-2" })
                            <div class="col-md-6">
                                @Html.DropDownListFor(c => c.DesignationID, Model.DesignationSelectListItem, "--SELECT--", new { @class = "form-control" })
                                @*@Html.ValidationMessageFor(c => c.DesignationId, null, new { @class = "text-danger" })*@

                            </div>
                        </div>

                        <div class="form-group">
                            <div class="col-sm-offset-2 col-sm-10">
                                <button type="submit" class="btn btn-success" onclick="LoginUser()" value="Save">Submit</button>
                            </div>
                        </div>

                    </div>
                    <div class="col-lg-3"></div>
                </div>

            </div>

        </form>
    </div>
    <script>

        var LoginUser = function () {
            debugger
            var data = $("#myForm").serialize();

            if (!$("#myForm").valid()) {

                return false;
            }


            // Ajax call here



        }

    </script>
    <script src="~/Scripts/jquery.validate.min.js"></script>
    <script src="~/Scripts/jquery.validate.unobtrusive.min.js"></script>
    <script src="~/Scripts/jquery-3.0.0.min.js"></script>
    <script src="~/Scripts/bootstrap.min.js"></script>

</body>
</html>